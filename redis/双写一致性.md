
# 允许延时一致的业务，采用业务通知
- 使用 MQ 中间件，更新数据后，通知缓存删除
- 利用 canal 中间件，不需要修改业务代码，伪装为 mysql 的一个从节点，canal 通过读取 binlog 数据更新缓存
  
# 强一致性，采用 Redisson 提供的读写锁

- 共享锁：读取 readLock，加锁之后，其他线程可以共享读操作。
- 排他锁：独占锁 writeLock 也叫，加锁之后，阻塞其他线程读写操作。