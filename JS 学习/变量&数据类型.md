
const 定义的是不可变的变量，可变的一般使用 let，var 现在不常用（过时）


**Number**
number 类型代表整数和浮点数。
除了常规的数据，还包括所谓的特殊数值也属于这种类型：Infinity、-Infinity、NaN。
- Infinity 代表数据概念中的无穷大。
- NaN 代表一个计算错误。
```
alert("not a number / 2"); //NaN 这样的除法是错误的
```
NaN 是粘性的。任何对 NaN 的进一步数学运算都会返回 NaN


**null 值** 
特殊的 null 值不属于上述任何一种类型
它构成了一个独立的类型，只包含 null 值。相比较于其他编程语言，JavaScript 中的 null 不是一个“对不存在 Object”的引用或者“null 指针”。
JavaScript 中的 null 仅代表一个“无”、“空”或“值未知”的特殊值。

**undefined 值**
特殊值 undefined 和 null 一样自成类型。undefined 的含义是未被赋值
如果一个变量已被声明，但未被赋值，那么它的值就是 undefined

**Object 类型和 Symbol 类型** 
Object 是一个特殊的类型。
其他所有的数据类型都被称为“原始类型”，因为它们的值只包含一个单独的内容。相反，object 则用于存储数据集合和更复杂的实体。
symbol 类型用于创建对象的唯一标识符。

typeof 运算符
typeof 运算符返回参数的类型。当我们想要分别处理不同类型值的时候，或者想快速进行数据类型校验时，非常有用
简单点说：typeof 是一个操作符，不是一个函数。这里的括号不是 typeof 的一部分。它是数学运算分组的括号



## 交互
alert
显示信息

prompt
显示信息要求用户输入文本。点击确定返回文本，点击取消或按下 ESC 键返回 null

confirm
显示信息等待用户点击确认或取消。点击确定返回 true，点击取消或按下 Esc 键返回 false。

这些方法都是模态的：它们暂停脚本的执行，并且不允许用户与该页面的其余部分进行交互，直到窗口被解除。

上述方法共有两个限制：
1.模态窗口的确切位置有浏览器决定。通常不在页面中心。
2.窗口的确切外观也取决于浏览器。我们不能修改它。


## 类型转换
js中非空的字符串总是true

注意：
- 对undefined进行数字转换时，输出结果为NaN，而非0.
- 对“0” 和只有空格的字符串进行布尔型转换时，输出结果为true。


## 函数
**局部变量 

在函数中声明的变量只在该函数内部可见
**外部变量**
函数对外部变量有全部的访问权限，也可以修改外部变量

如果在数据内部声明了同名变量，那么函数会遮蔽外部变量。

## 函数表达式

在js中，函数不是神奇的语言结构，而是一种特殊的值。
在上面函数使用的语法称为函数声明：
```
function sayHi(){
	alert("Hello");
}
```
另一种创建函数的语法称为 函数表达式。
它允许我们在任何表达式的中间创建一个新函数。
例如：
```
let sayHi = function() {
	alert("Hello");
}
```
由于函数创建发生在赋值表达式的上下文中（在 = 的右侧），因此这是一个函数表达式。

## 对象引用和复制

对象于原始类型的根本区别之一是，对象是”通过引用“存储和复制的，而原始类型：字符串、数字、布尔值等--总是作为一个整体复制。

### 通过引用比较

仅当两个对象为同一个对象时，两者才相等。
```
let a = {};
let b = a;
alert(a == b); //true
alert(a === b); //ture
```
而这里两个独立的对象并不相等，即使它看起来很像：
```
let a = {};
let b = {};

alert(a == b); //false
```

### 克隆于合并

```
Object.assign(dest, [src1, src2, src3...])
```
- 第一个参数 dest 是指目标对象。
- 更后面的参数 src1，...,srcN（可按需传递多个参数）是源对象。
- 该方法将所有源对象的属性拷贝到目标对象 dest 中。换句话说从第二个开始的所有参数的属性都被拷贝到第一个参数的对象中。
- 调用结果返回 dest

使用 const 声明的对象也是可以被修改的
通过引用对对象进行存储的一个重要的副作用是声明为 const 的对象可以被修改。
```
const user = {
 name: "John"
}

user.name = "Pete"; // (*)

alert(user.name);  //Pete
```



看起来（*） 行的代码会触发一个错误，但实际并没有。user 的值是一个常量，它必须始终引用同一个对象，但该对象的属性可以被自由修改

